#include <string>
#include <iostream>

using namespace std;

std::string get_middle(std::string input) 
{
  string str;
  
  int half = input.size() / 2;
  
  input.size() % 2 == 0 ? str = input.substr(half - 1, 2) : str = input.substr(half, 1);
    
  return str;
}
