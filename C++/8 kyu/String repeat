#include <string>
using namespace std;

std::string repeat_str(size_t repeat, const std::string& str) 
{
  string newStr;
  
  while (repeat) 
  {
    newStr.append(str);
    repeat--;
  }
  return newStr;
}
