#include <vector>
using namespace std;

std::vector<int> invert(std::vector<int> values)
{
    vector <int> copy;
    for(int number : values) 
    {
      copy.push_back(number * -1);
    }
    return copy;
}
